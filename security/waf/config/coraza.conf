# DevShield WAF configuration contract.
# The official Coraza CRS NGINX image loads its engine and CRS rule chain.
# Runtime mode and thresholds are passed as explicit environment variables by run.sh.
SecRequestBodyAccess On
SecResponseBodyAccess Off
SecAuditEngine RelevantOnly
SecAuditLogFormat JSON
SecAuditLogParts ABIJDEFHZ
