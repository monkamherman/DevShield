title = "DevShield Gitleaks configuration"

[extend]
useDefault = true

[allowlist]
description = "Repository-generated output and deliberately isolated security fixtures are tested separately."
paths = [
  '''(^|/)node_modules/''',
  '''(^|/)(dist|build|coverage|reports)/''',
  '''(^|/)tests/security/fixtures/''',
]
