# Applications

Phase 05 adds `fixture/`, a minimal dependency-free Node.js HTTP application used as the first deterministic container artifact. It is foundation validation code, not a production application architecture. The Dockerfile runs it as the non-root `node` user.
