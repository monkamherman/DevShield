# Applications

This area contains application components protected by DevShield. `backend/` is reserved for Node.js/Express services and `frontend/` supports React and/or Angular applications. No application implementation exists yet.
