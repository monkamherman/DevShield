# Deployment

Environment boundaries are reserved under `dev/`, `staging/` and `production/`. Actual deployment definitions will be introduced in later phases, with stricter trust requirements for production.
